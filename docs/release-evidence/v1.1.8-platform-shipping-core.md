# Store v1.1.8 / Platform v0.18.0 Shipping Core migration evidence

## Immutable input and boundary

- Previous Store release: `v1.1.7`.
- Platform pin: `v0.18.0` commit
  `3b8bdacc224bda625103718ba0fa8489517ff993`.
- Shipping Core: `0.1.0`.

Store consumes deterministic shipping profiles, mixed-shipment aggregation,
missing-metadata summaries, fallback/free/manual quote shapes, and option
selection. Store retains product policy, destination validation, USPS provider
configuration, credentials, requests, OAuth, cache, backoff, retries, checkout,
fulfillment, storage, deployment, and rollback.

## Characterization and reduction

- An independent consumer commit captured a mixed physical tier,
  support-item, and add-on shipment plus selected-option fallback before code
  moved.
- Existing Store shipping coverage exercises strict destination validation,
  missing metadata, fallback and free shipping, manual flat eligibility,
  domestic/international USPS quotes, timeouts, 401 refresh, rate limits,
  server failures, cache expiry, cooldown, and credential absence.
- The consumer shipping module is reduced by 524 duplicate lines. Its remaining
  provider transport and product orchestration are Store-owned.

## Required release evidence and rollback

- The full pre-merge gate passed: 427 unit tests and 34 browser tests passed,
  with every secret, content, i18n, syntax, focused regression, build-artifact,
  security, Podman resource, Worker, and browser phase green.
- Root and Worker production dependency audits reported zero vulnerabilities;
  the production Wrangler configuration bundled successfully in dry-run mode.
- A protected pull-request smoke remains required after Platform `v0.18.0`
  and preceding Store releases are merged.
- Roll back one Store commit and restore the v1.1.7 Platform pointer; no other
  consumer rollback is required.
