# Store v1.0.8 Release Evidence

Date opened: 2026-07-13

Release status: **approved for immutable release — final deploy and tag pending**

The Storefront package, Worker package, canonical platform version, request identifiers, changelog, roadmap, and Pool adaptation record identify this work as `1.0.8`. The published `v1.0.7` tag remains immutable. Every required closure row below now has direct evidence or an explicit owner-approved Pool-aligned disposition; deploy the exact final commit before creating the immutable `v1.0.8` tag.

## Scope

- Root repository operating guidance and docs-as-code alignment
- DRY optional add-on variant pricing
- Repository-authoritative image, video, and audio management and optimization evidence
- Stripe request, webhook, journal, and read-only reconciliation hardening
- Durable Resend outbox, delivery evidence, and suppression
- Production quality/admin follow-through
- Pool-aligned Cloudflare Workers Cache disabled/evidence-gated posture
- Protected backup, restore, and disaster-recovery operational proof

Tax expansion and the guided setup TUI remain outside `v1.0.8`.

## Roadmap audit

| Roadmap slice | Audit result | `v1.0.8` disposition |
| --- | --- | --- |
| Product, media, and add-on authoring | Implemented and locally verified | Moved from Future Features to completed storefront/catalog work |
| Payment integrity and reconciliation | Implemented and locally verified | Moved from Future Features to completed checkout/payment work |
| Media optimization reporting | Manifest and dashboard expose source/derivative bytes, dimensions/duration, optimization state, references, warnings, and budgets | Moved into completed media authoring |
| Podman alignment and troubleshooting | Production-like wrappers, resource gate, local-state reset, Stripe forwarding guidance, and merge checklist exist | Kept under completed local development/testing |
| Existing localization and SEO QA | Catalog completeness, rendered route/metadata evidence, source-hashed review packets, and accessibility/browser coverage exist | Kept under completed production-quality gates |
| New media-admin runtime copy | Store-owned labels, errors, filters, warnings, and media-type names use the existing runtime-message/i18n path; user-authored product and media data remains unchanged | Completed and locally verified in English and Spanish |
| Deployed SEO/admin-language sampling | Live English/Spanish home, product, and admin samples passed after deployment | Completed production gate |
| Workers Cache implementation and Orders decision | Complete; Orders failed its benefit gate and correctly remains disabled | Owner-approved Pool parity: every optional route stays disabled and evidence-gated; no speculative Analytics rollout or timed experiment is required for `v1.0.8` |
| Recovery automation and synthetic rehearsal | Complete and tested | Kept completed |
| Captured-data recurring recovery objective | Protected capture, off-account archive, live read-only Stripe comparison, preview readback, and cleanup passed | Owner-approved Pool parity: the immutable separate-account archive is the required durable copy; physical second-device copying remains an optional operator enhancement |

## Evidence baseline

### Local candidate

- The exact updated working-tree candidate passed an uninterrupted complete pre-merge gate on 2026-07-13: 14 focused files/84 tests, 79 unit files/375 tests, four security files/22 tests, Worker smoke, and 31 Playwright tests. Worker smoke proved the 23-product catalog and add-on APIs, valid canonical cart acceptance, tampered-cart rejection, and malformed-checkout fail-closed behavior.
- The deterministic media check examined 217 files and passed with 51 source assets, zero changed assets, zero missing derivatives, zero broken references, and a current manifest.
- The Store data inventory classified all 42 detected Worker storage families. Secret and English/Spanish i18n audits passed.
- Configuration synchronization, build, content, SEO, performance-budget, cache-policy, launch-readiness, resource, syntax, and diff-hygiene checks passed across all ten pre-merge phases.
- An initial attempt lost its Podman connection during the security phase. `npm run podman:doctor` restored the local runtime, the focused security suite passed, and the subsequent uninterrupted full rerun passed. This was a local runtime interruption, not an application assertion failure.
- The complete release-smoke wrapper passed on 2026-07-13 from exact code commit `7bb7319f0ced18c077ac4f68c6c9d7b368c10fe5`. Pre-merge, launch readiness, Podman doctor/E2E, automated accessibility coverage, rendered i18n/SEO, fulfillment, read-only provider readiness, payment readiness, representative restore rehearsal, and strict recovery readiness passed. The optional screen-reader transcript was explicitly skipped.
- Read-only provider evidence had zero failures, one warning, and two explicit skips: the Stripe test webhook lacked the expected event subscriptions, while DNS API record inspection and the Stripe live webhook endpoint lacked their dedicated local credentials. Public DNS, deploy-secret names, the Cloudflare token, four KV namespaces, one production R2 bucket, the Resend domain, and USPS quote fixtures were readable and passed. No live payment or provider mutation was made.
- Release-smoke recovery readiness passed six checks, warned only because its local synthetic invocation did not ingest the protected encrypted receipt, and failed none. The independent strict rerun with the current protected receipt passed seven checks with no warnings or failures.
- A fresh source-hashed localization packet reports all public shell, product metadata, checkout/order, email, terms, and admin files present and ready for human review; it contains neither credentials nor customer data and does not claim professional review.
- A read-only production-posture audit found no actionable failure: all required production secret names, bindings, explicit origins, the live app mode, super-admin posture, and provider evidence passed. The recommended dedicated `STORE_FULFILLMENT_SECRET` was added to the production Worker on 2026-07-13 without recording its value. The live Stripe destination was subsequently verified through the signed-in provider dashboard.

### Deployed provider and route evidence

- [Deploy Production run 29266322535](https://github.com/aindaco1/store/actions/runs/29266322535) deployed commit `7bb7319f0ced18c077ac4f68c6c9d7b368c10fe5`. The Worker deploy, Workers Cache purge, Pages deploy, Cloudflare zone purge, and live admin-response security verification all passed.
- [Release Provider Evidence run 29264378809](https://github.com/aindaco1/store/actions/runs/29264378809) passed strict Cloudflare DNS inspection and live English/Spanish admin response-rule verification with zero failures, warnings, or skips. [Production Posture run 29264680573](https://github.com/aindaco1/store/actions/runs/29264680573) passed with `STORE_FULFILLMENT_SECRET` present, zero provider failures, and no posture issue; [Localization Review run 29264680557](https://github.com/aindaco1/store/actions/runs/29264680557) passed and emitted a source-hashed human-review packet without claiming professional translation.
- Live `/`, `/es/`, and `/products/dust-wave-mug/` samples returned `200` with canonical URLs, English/Spanish/`x-default` alternates, Open Graph, Twitter cards, and the expected Organization/return-policy plus route-specific JSON-LD. `/admin/` and `/es/admin/` returned `200`, `noindex`, private/no-store/no-transform caching, and no report-only CSP.
- Unauthenticated product-media browse, media optimization, and payment-reconciliation mutations returned `401` with private/no-store responses. The unsigned Resend webhook returned `401 Invalid signature`.
- Resend webhook `8519bb9e-571d-470d-be10-7a3c7fecaf38` is enabled only for `email.delivered`, `email.bounced`, `email.complained`, `email.failed`, and `email.suppressed` at `https://checkout.dustwave.xyz/webhooks/resend`. Its signing secret was piped directly into the production Worker. A no-customer-data provider message produced a signed minimized `email.delivered` marker for provider message `0aa953f7-5093-426b-a17e-ceace7ee57df` at `2026-07-13T16:00:16.801Z`; the outbox queue was idle with no pending work at `2026-07-13T16:04:17.736Z`.
- Stripe Dashboard read-only evidence confirms the live `store-checkout` destination is active at `https://checkout.dustwave.xyz/webhooks/stripe`, listens only for `payment_intent.succeeded` and `payment_intent.payment_failed`, uses API version `2022-11-15`, and reports four deliveries with zero failures. Wrangler secret-name inventory confirms `STRIPE_WEBHOOK_SECRET` and `STRIPE_WEBHOOK_SECRET_LIVE` remain configured; the working signing secret was not exposed or rolled.
- The first deployed reconciliation cycle exposed 414 false breaks, all historical Snipcart imports: 386 missing Stripe PaymentIntent IDs and 28 legacy free-order status shapes. No provider read was unavailable, and the protected live comparison independently matched all six Stripe-backed orders. Commit `7bb7319` makes non-Stripe orders explicitly non-applicable and version-triggered one bounded index recheck. Algorithm v2 completed all 420 indexed orders at `2026-07-13T16:46:33.910Z` with zero open or unavailable results; an aggregate-only read confirmed all 414 prior break records are resolved and retained no discrepancy reasons. No Stripe mutation occurred.

### Protected recovery bootstrap

- The private Standard `store-recovery-archive` bucket was provisioned on 2026-07-13 in recovery account `009cd07ca32afba40cec52f571588d34`, distinct from production account `fee6147787ab9025e9fd8e6e8fd76910`. Its public development URL is disabled and it has no custom domain.
- `store-recovery-400d` is enabled for the `store/` prefix for 400 days. A non-sensitive canary uploaded successfully, returned byte-identical content, rejected deletion with HTTP 409, and returned the original bytes again after the rejected deletion.
- A dedicated account token is scoped to Object Read & Write for `store-recovery-archive` only. Its S3 credentials and bounded `s3://store-recovery-archive/store` URI are stored only as protected GitHub environment secrets; region `auto`, the credential-free recovery-account endpoint, provider, account ID, and lock duration are repository variables.
- An initially generated credential whose confirmation screen entered tool output was revoked before use. The active replacement values were transferred through the system clipboard directly into encrypted GitHub secrets and the memory-only canary process, then the clipboard was cleared; no plaintext credential file was created.

### Protected recovery execution

- [Quarterly Recovery Operations run 29265464555](https://github.com/aindaco1/store/actions/runs/29265464555) passed from commit `662fc9cbf807c5da77aaa2d0082d34b043ad9108` after a successful zero-error low-traffic gate and required `production-recovery` approval. A prior attempt stopped before data capture because Ubuntu 24.04 no longer exposes the `awscli` apt package; commit `662fc9c` now installs only `age` and asserts the hosted runner's AWS CLI v2.
- The age-encrypted snapshot captured 793 authoritative/control KV values across 19 families, one completely enumerated R2 object, and all six authenticated admin exports. Capture completed in 87.123 seconds with 31 KV lists, 16 KV bulk reads, 793 KV keys, one R2 read/141,969 bytes, and six admin-export reads.
- The 35,733,923-byte archive was copied to the separate-account `store/` prefix and read back byte-identically. The full drill took 158 seconds, verified 793 restored KV values and one R2 object, executed 44 planned preview actions with zero invalid or missing-value actions, and left zero residual preview KV/R2 records. Production writes, Stripe writes, and email operations were all zero.
- The restricted live-mode Stripe comparison examined all six Stripe-backed captured orders: six matches, zero mismatches, zero unavailable/not-found provider objects, and no truncation. The recovered snapshot contained 420 confirmed orders, 33 sold SKUs, and aggregate sold quantity 643 without emitting order/provider identifiers or customer data.
- The sanitized evidence artifact digest is `sha256:1c0bbdf19e69efc8d1d5c1abf5f98bb73b428aeea73ba0f5cb76e83e81cf6228`. The secondary 90-day encrypted GitHub artifact digest is `sha256:c5f1408ba0e988ca8881755e9172565c7fd237d16ca599b60c95f5768be16cf1`; its receipt records archive SHA-256 `b9f25e59c3f9523051a7a09321b3c84ce0c3ebecb55be83543174ae989f34785`. The off-account locked R2 copy, not the GitHub artifact, is the durable archive.
- The one-time admin token was consumed only in protected memory, then deleted from the GitHub environment; `RECOVERY_DRILL_ENABLED` was reset to `false`. Strict readiness rerun against the current encrypted receipt passed seven checks with zero warnings or failures and a snapshot age of 0.05 hours.

### Existing production operations

- [Production Posture run 29250756335](https://github.com/aindaco1/store/actions/runs/29250756335) passed on `main` on 2026-07-13. It does not cover the undeployed `1.0.8` candidate.
- [Recovery Readiness run 29190061207](https://github.com/aindaco1/store/actions/runs/29190061207) passed six checks, warned that current encrypted snapshot-receipt evidence was unavailable, and failed none. Its synthetic rehearsal restored representative Store state without production data or provider writes.
- [Quarterly Recovery Operations run 29170532958](https://github.com/aindaco1/store/actions/runs/29170532958) passed a 15-minute preflight with 20 requests, four subrequests, and zero errors. The protected captured-data restore remained skipped, so the recurring objective is not proven.
- [Workers Cache Evidence run 29249303150](https://github.com/aindaco1/store/actions/runs/29249303150) failed with no evaluated routes because all optional cache routes were disabled; its scoped Orders probe also correctly returned `DISABLED` rather than the `HIT` required by the current strict check. This is an evidence-state defect, not proof that an unmeasured route should be enabled.
- [Workers Cache Evidence run 29265014453](https://github.com/aindaco1/store/actions/runs/29265014453) proves the `v1.0.8` evidence-state fix: the disabled Orders probe passed all seven sanitization, budget, route, consistency, unchanged-repeat, and zero-repeat-read checks. Aggregate acceptance correctly reported `inconclusive/deployment_warmup` at 0.12 hours. On 2026-07-13 the owner approved Pool parity for this release: every optional route remains disabled and evidence-gated, and no Analytics enablement or additional timed experiment is required for `v1.0.8`.
- The exact final working-tree release-smoke evidence was generated at `2026-07-13T17:15:38Z`. All required phases passed. Local provider probes reported zero failures, one warning, and two skips because the Pool test endpoint is not a Store endpoint and the CLI lacked live endpoint access; the signed-in Stripe Dashboard subsequently closed the live Store endpoint proof. Payment mutation skips were intentional, while the passed Podman Worker/E2E and payment unit contracts covered the local boundary without creating a live charge.

## Closure sequence

### 1. Candidate code and documentation closure

Owner: release engineer

Actions:

1. Extract all new media picker/upload/replace/repair/filter/warning/status copy into the existing runtime-message and `_data/i18n/*` catalogs.
2. Add matching English/Spanish placeholders and focused browser/unit assertions; verify meaningful image alt text and explicit decorative state in both languages.
3. Update only owning docs when a behavior or operator contract changes.
4. Run `npm run media:optimize:check`, `npm run backup:inventory:audit`, `npm run test:secrets`, `npm run test:i18n`, `npm run test:premerge`, and `git diff --check`.
5. Run `npm run release:smoke -- --evidence-file <private-or-sanitized-path>` with available production-read credentials. Record every credential-dependent skip instead of inferring a pass.

Acceptance:

- No new media-admin customer/operator copy bypasses the existing i18n path.
- All automated gates pass from the exact candidate commit.
- Evidence contains no secret values, login tokens, customer data, provider payloads, or response bodies.

### 2. Review, provider configuration, and manual deployment

Owner: Store production operator

Actions:

1. Review and merge the exact tested candidate. Run config sync and Worker deploy dry-run; confirm `EMAIL_OUTBOX_ENABLED=true`, `PAYMENT_RECONCILIATION_ENABLED=true`, and the expected bindings.
2. Create `https://checkout.dustwave.xyz/webhooks/resend` for delivered, bounced, complained, failed, and suppressed events. Store `RESEND_WEBHOOK_SECRET` in Cloudflare without reading it into evidence.
3. Confirm Stripe webhook subscriptions and pinned API behavior with read-only checks. Do not create a live charge for readiness proof.
4. Dispatch **Deploy Production** manually from the reviewed commit and record the exact Pages and Worker deployment identifiers.
5. Verify public/admin cache boundaries, unsigned Resend webhook rejection, unauthenticated media/reconciliation rejection, and private/no-store admin responses.
6. Send a Resend provider test message with no customer data and record the signed delivery event, 35-day dedupe marker, and minimized delivery row.
7. Review the first outbox and payment-reconciliation cycles for queue state, open breaks, cron heartbeat, and redacted logs.

Acceptance:

- The exact reviewed commit is deployed; provider routes fail closed; a signed Resend delivery event is persisted; the first reconciliation batch is read-only and has an operator disposition for every critical break.

Rollback:

- Use the documented Worker/Pages rollback and config kill switches. Disabling outbox or reconciliation stops new scheduled work without changing committed order truth. Never repair a failed rollout with an ad hoc Stripe charge action.

### 3. Cloudflare Workers Cache decision — approved

Owner: Store performance operator

Actions:

1. **Implemented in the release candidate:** “no optional cache route enabled” is an explicit successful `not_applicable`/`no_enabled_candidates` state only after the authenticated request succeeds and sanitization, three-read budget, route matching, consistent `DISABLED`, unchanged-repeat, and zero-repeat-read assertions pass. `WORKERS_CACHE_EVIDENCE_ROUTE` selects the sequential candidate. Malformed data, leaks, unsafe or mismatched routes, excess requests, mixed enabled/disabled evidence, or an enabled candidate that misses its gate fail closed.
2. Add focused tests for disabled/no-candidate, deployment warmup, insufficient traffic, enabled-pass, enabled-fail, and sanitized artifact behavior.
3. **Owner-approved 2026-07-13:** follow Pool's release posture. Keep Orders, Analytics, order-derived inventory, and download readiness disabled; do not perform an enablement experiment merely to satisfy the release.
4. Retain the sequential 30-sample/40-percent p95 gate, mutation freshness checks, and kill switches for any future proposal to enable a route.

Acceptance:

- The scheduled job truthfully passes when caching is intentionally inapplicable and fails when an enabled route violates policy.
- No optional route is enabled. Future enablement remains blocked on sanitized 30-sample evidence and an explicit pass decision.
- `cross_version_cache` remains false unless separately proven safe and beneficial.

Rollback:

- Disable the route switch, deploy the reviewed config, purge the affected policy/tag, verify uncached freshness, and retain sanitized evidence under the incident procedure.

### 4. Protected recovery proof

Owner: Store recovery operator and required protected-environment reviewer

Actions:

1. **Completed on 2026-07-13:** following Pool's pattern, provision a private Standard-class R2 archive in a Cloudflare account separate from production. Credentials are Object Read & Write for the Store bucket only, the destination uses the `store/` prefix, no public URL or custom domain is enabled, and the prefix has a 400-day immutable lock.
2. **Implemented in the release candidate:** before customer-data capture, require distinct production/archive account IDs, a matching Cloudflare R2 endpoint, region `auto`, a Store-only prefix, and exactly 400 lock days; then upload a non-sensitive canary, verify byte-identical readback, require deletion rejection, and verify post-rejection readback. After the target exists, retain the resulting sanitized proof.
3. **Completed 2026-07-13:** create a fresh one-time super-admin token immediately before capture and run a complete encrypted `v1.0.8` snapshot including admin CSV/readiness/download exports and complete KV/R2 enumeration.
4. **Completed 2026-07-13:** record snapshot duration plus aggregate KV/R2/admin-export operations and compare them with the approved four-hour active-sales RPO and documented RTO.
5. **Owner-approved 2026-07-13:** match Pool's release posture by treating the verified, immutable separate-account R2 archive as the required durable off-account copy. Keep `npm run backup:offsite` available for an optional later physical second-device copy.
6. **Completed 2026-07-13:** run the aggregate captured-order comparison with the provisioned restricted live-mode Payment Intents read-only Stripe key; mode matched and no Stripe write occurred.
7. **Completed 2026-07-13:** enable the protected quarterly job only after its environment, reviewer, age identity, fresh token, restricted Stripe key, off-account destination, and preview KV/R2 resources are verified; disable it and delete the token after execution.
8. **Completed 2026-07-13:** execute preview-only restore, value/object readback, exact-snapshot cleanup, zero-residual verification, and best-effort failure cleanup coverage without touching production.
9. **Completed 2026-07-13:** run recovery readiness again with the current encrypted receipt and record no warning for snapshot age.

Acceptance:

- One immutable off-account encrypted release snapshot exists, passed byte-identical readback, and is recoverable through the protected preview drill; a physical second-device copy is not a `v1.0.8` release gate.
- The protected drill includes admin exports and live read-only Stripe comparison, restores only approved preview state, excludes quarantine/derived state as designed, and leaves zero snapshot-owned preview data.
- Duration/operation evidence supports the approved RPO/RTO or records an owner and dated corrective action.

Rollback/safety:

- Keep `RECOVERY_DRILL_ENABLED=false` until every prerequisite is present. The automated workflow never targets production restore. Production recovery remains a manual incident process with maintenance, paused Stripe webhooks, inventory review, a distinct verified pre-restore snapshot, maker/checker controls where required, and exact acknowledgement.

### 5. Finalize the immutable release

Owner: release maintainer

Actions:

1. Complete the evidence table below with exact commits, run URLs, artifact digests, deployment identifiers, owners, and timestamps.
2. Confirm the roadmap contains no completed item under Future Features and no open item marked complete.
3. Replace `Unreleased` with the actual release date in `CHANGELOG.md`.
4. Create immutable tag `v1.0.8` at the exact reviewed/deployed commit and publish release notes derived from the changelog.
5. Rerun production posture, cache evidence, and recovery readiness against the tagged deployment; link the results without rewriting the tag.

## Release evidence checklist

| Gate | Required state | Evidence |
| --- | --- | --- |
| Version/package alignment | `1.0.8` across root, Worker, locks, config, request identifiers, docs | Pass locally on 2026-07-13; package-lock dry runs and config sync passed |
| Full pre-merge | Pass on exact candidate | Pass locally on 2026-07-13; 14 focused files/84 tests, 79 unit files/375 tests, four security files/22 tests, Worker smoke, and 31 Playwright tests |
| Release smoke | Pass; credential gaps explicit | Pass locally on 2026-07-13; all required phases passed, optional screen-reader transcript skipped; provider result 0 fail/1 warn/2 skip; recovery result 6 pass/1 warn/0 fail |
| Resend delivery webhook | Signed provider event; minimized row and 35-day marker | Pass on 2026-07-13; Store-only endpoint enabled for five required events, unsigned request rejected, signed `email.delivered` marker persisted |
| First payment reconciliation | Read-only cycle reviewed | Pass; six live Stripe orders matched, algorithm v2 completed all 420 indexed orders, and all 414 historical Snipcart false breaks are resolved with zero open/unavailable results |
| Production deployment | Exact reviewed commit | Pass; run 29266322535 deployed `7bb7319f0ced18c077ac4f68c6c9d7b368c10fe5` |
| Live route/security sample | Public success; private/unsigned routes fail closed | Pass on 2026-07-13 for English/Spanish public/admin routes, SEO metadata, private admin cache policy, unauthenticated media/reconciliation, and unsigned Resend |
| Cache evidence semantics | Disabled/no-candidate state truthful and tested | Pass locally on 2026-07-13; disabled/no-candidate passes only after the authenticated fail-closed probe, while leaks, budget violations, and mixed enabled/disabled evidence fail |
| Cache candidate decision | No route enabled without evidence | Approved Pool-aligned no-rollout: run 29265014453 passed disabled-route safety; Orders, Analytics, inventory, and downloads remain disabled and evidence-gated, with no timed enablement experiment required for `v1.0.8` |
| Off-account archive bootstrap | Scoped, private, encrypted, immutable, byte-verified | Infrastructure pass on 2026-07-13: separate private Standard bucket, bucket-only Object Read & Write credentials, Store prefix, 400-day lock, byte-identical canary readback, HTTP 409 deletion rejection, and post-rejection readback; first encrypted release archive remains part of the captured-snapshot gate |
| Captured-data snapshot | Admin/KV/R2 complete, encrypted, current receipt | Pass in run 29265464555: fresh `v1.0.8` capture with 793 KV values, one complete R2 object, six admin exports, age encryption, off-account verification, and a 400-day Store-prefix lock |
| Physical second-device copy | Optional enhancement under Pool-aligned release posture | Approved non-release gate on 2026-07-13; the separate-account age-encrypted archive has byte-identical readback, a 400-day lock, and successful protected preview recovery |
| Live Stripe comparison | Restricted read-only, correct mode, aggregate only | Pass: live mode, six of six matches, zero Stripe writes, mismatches, unavailable, or not-found objects |
| Protected preview drill | Restore/readback/cleanup pass, zero residual | Pass in run 29265464555: 793 KV values and one R2 object read back; zero residual KV/R2 records |
| Final readiness/posture | No unexplained failure; warnings owned and dated | Current receipt readiness pass: 7 pass/0 warn/0 fail; cache/offsite dispositions are owner-approved, and final tagged posture/readiness reruns remain |

## External provisioning status

The owner authorized and signed into the same separate recovery Cloudflare account used by Pool. The Store-specific bucket, lock, scoped credentials, protected GitHub secrets, repository variables, non-sensitive immutability canary, captured encrypted snapshot, live read-only Stripe comparison, and protected preview restore are complete. Do not substitute the production account or Pool's bucket/token. The one-time admin token was deleted after use and the protected workflow is disabled again. On 2026-07-13 the owner approved Pool-aligned release posture: optional cache routes remain disabled/evidence-gated, and a physical offsite-PC copy is an enhancement rather than a release blocker.
