# Store v1.1.4 Stripe test webhook evidence

Date: 2026-08-06

## Boundary

- Stripe endpoint: `we_1U1TeqK9nUOzi4tvMpVMmPVE`
- Mode: test (`livemode=false`)
- Status: enabled
- URL: `https://store-worker-staging.jogo.workers.dev/webhooks/stripe`
- Events: `payment_intent.succeeded`, `payment_intent.payment_failed`
- Cloudflare Worker: `store-worker-staging`
- Cloudflare storage: staging-only Durable Object namespace, KV namespaces
  `2ccafeb7178c4c7b8a0bd5d548eb6e5b` and
  `d8c411dbc8bb4de5b7df20864dc923a9`, and R2 bucket
  `store-downloads-staging`
- Routes and cron: none
- External side effects: Resend dry run; USPS, payment reconciliation,
  runtime analytics, Workers Cache, and repository writes disabled

The Stripe test secret key, publishable key, webhook signing secret, and
staging application signing secrets are Cloudflare secrets. Their values were
not written to the repository or release evidence. Staging does not contain
production Resend, USPS, Turnstile, GitHub, or Store signing credentials.

## Automated evidence

- `npx wrangler deploy --env staging --dry-run --config worker/wrangler.toml`
  completed without configuration warnings.
- `npm run release:providers -- --no-dev-vars` found both the live production
  endpoint and dedicated test endpoint with the required event subscriptions;
  it reported zero failures and zero warnings.
- `PAYMENT_SMOKE_ALLOW_MUTATION=1 PAYMENT_SMOKE_CONFIRM=1
  PAYMENT_SMOKE_SCENARIOS=paid-digital npm run release:payment-smoke --
  --no-dev-vars` targeted the explicit staging Worker and production public
  site URL. It reported zero failures, warnings, or skips.
- The smoke created test PaymentIntent
  `pi_3U1TgoK9nUOzi4tv1VaGZubU` for isolated order
  `store-order-d8d406ca-7b06-4dab-a9b1-01d2fb055d4c`, verified its Store
  metadata, confirmed it with Stripe's test payment method, and observed
  provider-originated settlement to `confirmed` / `succeeded`.
- The settled order reported customer email `sent=true`, `dryRun=true`, and an
  empty error; no administrator recipient existed in staging and no email was
  sent to Resend.

No production order, payment, inventory, KV, R2, email, route, cron, or Worker
secret was mutated by this test.

## Rollback

Disable the Stripe test endpoint first, then roll the named staging Worker back
to its previous Cloudflare version or delete only `store-worker-staging` and
its three explicitly named staging storage resources. Production Store remains
on `store-worker`, `checkout.dustwave.xyz`, and its independent storage and
secrets throughout; a staging rollback requires no Pool, Platform, Podcast, or
production Store rollback.
