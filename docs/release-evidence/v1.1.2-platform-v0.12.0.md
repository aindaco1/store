# Store v1.1.2 Platform v0.12.0 migration evidence

## Scope

- Previous Store release: `v1.1.1`.
- Previous Platform pin: `7c5d4e8b44b25b9a649920c5a56d120ca29fc06f`
  (`v0.11.5`).
- Reviewed Platform pin: `09cc3cf1a35950fd410c652de390d44e71dd6fee`
  (`v0.12.0`).
- Extracted only the Pool/Store exact duplicates for header navigation, live
  announcements, timezone discovery, the New Mexico GRT starter reference and
  updater, and generated CSS/JavaScript minification.

Store continues to own all templates, labels, routes, styles, catalog and
product content, tax-provider and taxability policy, checkout, order and
inventory truth, fulfillment, configuration, credentials, storage, builds,
and deployment.

## Migration verification

- The Platform release source was byte-for-byte compared with both consumer
  implementations before the local copies were removed.
- Existing Store behavior tests now execute the pinned Platform source through
  the same consumer fixtures.
- `tests/unit/platform-pin.test.ts` locks the canonical remote, exact commit,
  exact workspace/package versions, and every consumed source path.
- Standalone local `npm run test:premerge` passed every phase after the
  competing Pool Podman stack was stopped: 415 unit and 34 browser tests,
  content/i18n/secrets, clean build/minification/budgets, security, resource,
  and Store Worker smoke all passed.
- Protected PR checks, production deployment, and live storefront/Worker smoke
  remain required before the release is complete.

## Independent rollback

Revert the single Store migration commit and redeploy immutable `v1.1.1`.
That restores the prior Platform pin and every local duplicate atomically;
Pool, Dust Wave, Podcast, and Platform remain unchanged. Run the focused
Platform pin and release-version contracts before deploying the rollback.
