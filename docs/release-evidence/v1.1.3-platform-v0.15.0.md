# Store v1.1.3 / Platform v0.15.0 migration evidence

## Immutable inputs

- Previous Store release: `v1.1.2`.
- Previous Platform pin: `09cc3cf1a35950fd410c652de390d44e71dd6fee`
  (`v0.12.0`).
- Reviewed Platform pin: `2e79a8d70cb6d30805ea141e53d32f9387441756`
  (`v0.15.0`).
- Platform package versions: workspace `0.15.0`, `@dustwave/worker-core`
  `0.6.0`, and `@dustwave/release-core` `0.1.0`.

## Migration boundaries

Store consumes the characterized shared HTTP/security response, timezone/date,
Stripe transport, Wrangler inventory, KV transformation, checksum, command
result, and provider-evidence primitives through thin local adapters. Store
continues to own its default private origin, Stripe API version and User-Agent,
catalog, checkout, order/inventory truth, fulfillment, routes, storage,
credentials, provider calls, builds, deployment, and rollback.

Payment-event and reconciliation lease identifiers now use Platform's Web
Crypto token primitive and fail closed instead of falling back to
`Math.random()`. The Platform submodule contains no Store credentials,
environment IDs, customer data, product content, or deployment authority.

## Pre-merge verification

- Focused adapter characterization passed for the Platform pin,
  validation/security, timezone/DST, Stripe, payment integrity, reconciliation,
  release-provider evidence, backup/restore helpers, release-version identity,
  and sandboxed deployment setup.
- `npm run test:premerge` passed every phase: tracked-secret, product-content,
  i18n, and syntax gates; 419 unit tests; production
  build/minification/budgets; security; Podman resources; Store Worker smoke;
  and all 34 responsive browser tests.
- Wrangler production dry-run bundled the Worker successfully with the
  reviewed KV, R2, Analytics Engine, and Durable Object bindings and made no
  deployment.
- Read-only provider evidence reported zero failures, one warning, and five
  explicit skips. Public DNS, GitHub deploy secret names, Cloudflare KV/R2
  discovery passed. The Stripe CLI test account did not expose the required
  Store webhook event set, while the direct local signed-webhook smoke passed;
  live CLI authentication had expired, so the production endpoint read was
  not repeated locally.
- Wrangler independently confirmed every required/recommended production
  Worker secret name and captured pre-deploy Worker version 103
  (`985100e1-539d-40d7-8d97-687310221223`) for rollback.

Protected pull-request checks and production deployment from the reviewed
immutable Store release ref remain required. After deployment, verify the live
storefront/Worker, confirmed mug inventory projection, security headers, and
responsive surfaces. Live Stripe endpoint verification remains read-only and
must not create a payment.

## Independent rollback

Revert the single Store migration commit and redeploy immutable `v1.1.2`. This
restores Platform `v0.12.0` and Store's prior local adapters atomically. Pool,
Dust Wave, Podcast, and Platform do not need to change. Re-run the focused
Platform-pin and release-version contracts before the rollback deployment.
